// Enum tipi controlli mappa per gestione stato pannello attivo
export enum TypeControls {
    SETTINGS = "settings",
    SEARCH = "search",
    COORDINATES = "coordinates",
    CHATBOT = "chatbot",
    LAYER_MANAGER = "layer-manager"
}
